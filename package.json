{
  "name": "stardeck-ppt-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-assets.mjs && vite --host 0.0.0.0",
    "build": "node scripts/prepare-assets.mjs && node scripts/clean-build.mjs && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "html2canvas": "1.4.1",
    "pptxgenjs": "^4.0.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.44.0",
    "vite": "^8.1.4",
    "wrangler": "4.110.0"
  }
}
